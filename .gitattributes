# AetherOracle — line-ending normalization.
# Windows launchers must be CRLF on checkout regardless of the committer's OS.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Everything else: native line endings.
* text=auto
