# frozen_string_literal: true

# Local memory / logs — never shipped.
.tm-ai/
*.db
*.sqlite
*.sqlite3
*.log

# Bundled gems (per-machine, platform-specific — never committed)
ruby/.vendor_bundle/
ruby/vendor/
ruby/.bundle/
ruby/.portable/

# Built gem artifact (regenerated by `gem build`)
*.gem

# OS cruft
.DS_Store
Thumbs.db
